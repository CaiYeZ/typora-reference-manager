## 0.1.19 更新内容

- 新增选中链接收藏：选中完整 Markdown 链接或正文中已渲染链接的文字后，按默认快捷键 `Alt+Ctrl+R`，或执行“引用管理器：新增常用引用”，自动填入名称和地址。
- 弹窗内可修改名称和地址；保存后立即可通过 `/fav` 搜索，取消不会保存，也不会修改正文。
- 相对文件地址按当前文档目录转换为绝对路径，便于跨文档复用。
- 新增“选中链接自动填充”设置，默认开启；关闭后新增命令和快捷键打开空白弹窗。
- 新增“新增常用引用快捷键”设置，修改立即生效并解除旧快捷键；留空恢复 `Alt+Ctrl+R`，不能与手动刷新快捷键相同。
- 已有常用引用保持兼容。设置页“新增”始终打开空白弹窗；不支持图片语法和引用式链接。

## 安装与升级

下载 `typora-reference-manager-v0.1.19.zip`，关闭 Typora 后解压，将其中的 `typora-reference-manager` 文件夹放入 `%USERPROFILE%\.typora\community-plugins\plugins\`，替换旧版运行文件并重启。

本次在同一版本 0.1.19 内更新安装包；已下载早期 0.1.19 的用户请重新下载。安装包只包含 `main.js`、`manifest.json`、`style.css`，同时提供 SHA-256 校验文件。GitHub 自动生成的 Source code 压缩包对应原始版本标签，安装请使用上述版本安装包。最新源码见 main 分支。

AI 辅助开发项目：由作者提出需求，通过 ChatGPT / Codex 辅助编写代码、迭代功能和整理发布文档。
